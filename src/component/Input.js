import React from 'react';
import { View } from 'react-native';

const Input = (Props) => {
  const { viewStyle } = styles;
  return (
    <View style={viewStyle}>
      
    </View>
  );
};

const styles = {
  viewStyle: {
    borderRadius: 5,
    borderWidth: 2,
    borderColor: '#00b894',
    padding: 10,
    margin: 10
  }

};
export { Card };
